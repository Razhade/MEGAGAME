using UnityEngine;

public class Casser : MonoBehaviour
{
    public float babouche;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        babouche = 10f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
